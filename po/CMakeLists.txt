string(REPLACE ";" "\n" TRANSLATION_SOURCE
    "${CORE_HEADERS};${CORE_SOURCE};${GAME_HEADERS};${GAME_SOURCE}")

string(REPLACE "${CMAKE_SOURCE_DIR}/" "" TRANSLATION_SOURCE "${TRANSLATION_SOURCE}")

GettextTranslate()
