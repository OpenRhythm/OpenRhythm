#pragma once

// Platforms
#define PL_WINDOWS ${PL_WINDOWS}
#define PL_LINUX ${PL_LINUX}
#define PL_OSX ${PL_OSX}

#define PLATFORM ${PLATFORM}

#define OSX_APP_BUNDLE ${OSX_APP_BUNDLE}

#if PLATFORM==PL_WINDOWS
    #define PLATFORM_WINDOWS
    #define PATH_SEP '\\'
    #define HOMEPATH_NAME "MusicGame"
#elif PLATFORM==PL_LINUX
    #define PLATFORM_LINUX
    #define PATH_SEP '/'
    #define HOMEPATH_NAME ".musicgame"
#elif PLATFORM==PL_OSX
    #define PLATFORM_OSX
    #define PATH_SEP '/'
    #define HOMEPATH_NAME "MusicGame"
    #define APP_NAME "musicgame.app" // add to cmake command?
#endif


// will add version info here later on